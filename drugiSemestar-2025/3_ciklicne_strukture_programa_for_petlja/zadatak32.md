```python
# Zadatak 32
# Саставити програм који исписује све троцифрене бројеве код којих је друга цифра за 4 већа од прве,
# а трећа за 3 већа од друге.

# Исписујемо наслов за резултате
print("Бројеви")

# Користимо три угњеждене петље за генерисање свих могућих троцифрених бројева
# Прва петља (a) - прва цифра троцифреног броја (1-9)
for a in range(1, 9 + 1, 1):
    # Друга петља (b) - друга цифра троцифреног броја (0-9)
    for b in range(0, 9 + 1, 1):
        # Трећа петља (c) - трећа цифра троцифреног броја (0-9)
        for c in range(0, 9 + 1, 1):
            # Формирамо троцифрени број: abc = 100*a + 10*b + c
            broj = 100 * a + 10 * b + c

            # Проверавамо да ли број задовољава услове:
            # 1. Друга цифра (b) је за 4 већа од прве (a)
            # 2. Трећа цифра (c) је за 3 већа од друге (b)
            if b == a + 4 and c == b + 3:
                # Исписујемо број који задовољава оба услова
                print(broj)
                # Напомена: Због ограничења (a≤9, b≤9, c≤9), само неколико бројева
                # може задовољити услове, јер ако је b=a+4, a c=b+3, онда
                # треба да буде c=a+7, што значи да за већину a,
                # вредности b или c ће бити ван дозвољеног опсега 0-9

```
